[package]
name = "keywardd-stub"
description = "Fake daemon speaking the ARCHITECTURE.md §7 protocol with canned data, so the desktop apps can be built before the real daemon exists."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true

[[bin]]
name = "keywardd-stub"
path = "src/main.rs"

[dependencies]
keyward-core = { path = "../keyward-core" }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
