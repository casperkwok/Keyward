[package]
name = "keyward-core"
description = "Vault model, reference grammar and policy engine for Keyward. Holds no secrets and performs no I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
